@page
@model WebApplication2.Pages.Drinks.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Drink[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Drink[0].CafeinePer100ml)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Drink[0].DefaultSize)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Drink[0].Ownership)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Drink) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CafeinePer100ml)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DefaultSize)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ownership)
            </td>
            <td>
                    @if (item.Ownership == DrinkOwnership.Own)
                    {
                        <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a>
                        <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
                    }
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a>
            </td>
        </tr>
}
    </tbody>
</table>
